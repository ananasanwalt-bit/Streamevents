# StreamEvents production container
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

# Use npm install so a package-lock.json is not required
RUN npm install --omit=dev

COPY . .

ENV NODE_ENV=production
ENV PORT=10000

EXPOSE 10000

CMD ["npm", "start"]
